throw new Error(
  `Do not import from samepage directly. Instead import the *method* you need directly. For example: 
    
import setupSamePageClient from "samepage/protocols/setupSamePageClient;"`
);

export {};
