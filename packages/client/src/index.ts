throw new Error(
  `Do not import from @samepage/client directly. Instead import the *method* you need directly. For example: 
    
import setupSamePageClient from "@samepage/client/protocols/setupSamePageClient;"`
);
